{{ form_start(form) }}
<div class="alert bgc-primary-l4 brc-primary-m4 d-flex align-items-center text-dark-tp2" role="alert">
    <i class="fas fa-info-circle mr-3 fa-2x text-blue"></i>

    <div>
        Esta es la información base del curso, luego podrás añadir el temario, foros y demás opciones en la página de detalle del curso.        
    </div>
</div>
<div class="h-1 my-3"></div>

    <div class="form-group row">
        <div class="col-sm-3 col-form-label text-sm-right pr-0 {{ form.headerImage.vars.errors|length > 0?'text-danger-m1' }}"><label class="mb-0 required" for="course_headerImage">Imagen</label></div>
        <div class="col-sm-9">            
            {% if course.headerImage %}
            <div class="imgBlock col-sm-4 col-md-6 pr-0 pl-0">                
                <div class="pos-rel d-style my-3 radius-1 shadow-sm overflow-hidden bgc-primary-m3" style="max-width: 200px;">
                    <a href="#" class="show-lightbox">
                        <img src="{{ asset('uploads/courses/' ~ course.headerImage) }}" class="w-100 d-zoom-2">
                    </a>

                    <div class="mt-hover position-tl w-100 bgc-black-tp4 p-25">
                        <div class="d-flex justify-content-center action-buttons text-110">                            
                            <a href="#" class="imgChangeBtn mx-2" data-id="course_headerImage"><i class="fa fa-pen text-orange-m2"></i></a>
                            <a href="#" class="imgDeleteBtn mx-2" data-id="course_headerImage" data-url="{{ path('course_deleteHeaderImage', {'id':course.id}) }}"><i class="fa fa-times text-danger-m3"></i></a>
                        </div>
                    </div>
                </div>                
            </div>
            {% endif %}
            <div class="imgFieldBlock col-sm-8 col-md-6 pr-0 pl-0" style="display: {{ course.headerImage?'none' }}">
            {{ form_widget(form.headerImage, {'attr':{'class':'ace-file-input'}}) }} 
            {{ form_errors(form.headerImage) }}
            {% if course.headerImage %}<small class="imgFieldCancelBlock"><a href="#" class="imgCancelBtn">Cancelar cambio de imagen</a></small>{% endif %}
            </div>            
        </div>        
    </div>
    
    {{ form_row(form.title) }}                
    {{ form_row(form.level) }}
    {{ form_row(form.description, {'attr':{'rows':8}}) }}
    
    <hr>
    
    <div class="form-group row">
        <div class="col-sm-3 col-form-label text-sm-right pr-0"><label class="mb-0" for=""></label></div>
        <div class="col-sm-9">
            <div class="alert alert-secondary bgc-default-l4 brc-secondary-m3 col-lg-6" role="alert">                
                Este es el precio base del curso, luego podrás aplicar ofertas que modificarán este precio
            </div>
        </div>        
    </div>
    <div class="form-group row">
        <div class="col-sm-3 col-form-label text-sm-right pr-0"><label class="mb-0" for="course_price">Precio base</label></div>
        <div class="col-sm-9">
            <div class="input-group">
                {{ form_widget(form.price, {'attr':{'class': 'col-sm-8 col-md-2 d-inline-block form-control text-right'}}) }}
                <div class="input-group-append">
                    <span class="input-group-text">€</span>
                </div>
            </div>            
        </div>        
    </div>
    
    <div class="my-5 border-t-1 brc-grey-l1 bgc-grey-l3 py-3">
        <div class="offset-md-3 col-md-9">
            <button class="btn btn-info" type="submit">
                <i class="fa fa-check mr-1"></i>
                {{ button_label|default('Guardar') }}
            </button>

            &nbsp;             
            {% if button_label|default('null') == 'Actualizar' %}
                <a href="{{ path('course_show', {id: course.id}) }}">Volver</a>
            {% else %}
                <a href="{{ path('course_index') }}">Volver al listado</a>
            {% endif %}
        </div>
    </div>
{{ form_end(form) }}
